# Вводим значение n
n = int(input("Введите натуральное число n: "))

# Проверка на корректность ввода
if n <= 0:
    print("n должно быть натуральным числом (больше нуля).")
else:
    product = 1  # Переменная для хранения произведения
    i = 1  # Начинаем с 1

    # Цикл с предусловием
    while i <= n:
        product *= (i ** 3)  # Умножаем на куб текущего числа
        i += 1  # Переходим к следующему числу

    # Выводим результат
    print(f"Произведение кубов чисел от 1 до {n} равно {product}.")
